<% content_for :title, @competency.dig("metadata", "title") %>

<div class="row justify-content-center">
  <div class="col col-lg-6">
    <div class="card border-0 mb-3" style="background: none">
      <div class="card-body">
        <%= image_tag @competency.dig("metadata", "logo_url"), alt: @competency.dig("metadata", "provider_name"), class: "float-end me-3 mt-3", width: 100 %>
        <h1 class="card-title"><%= @competency.dig("metadata", "title") %></h1>
        <%= link_to "View Original Data", params.fetch(:id), target: "_blank" %>
      </div>
    </div>

    <div class="details-view bg-white rounded shadow-sm">
      <div class="details">
        <h6>Competency details</h6>

        <div class="table-wrapper">
          <table class="table">
            <tbody>
            <% @competency.fetch("properties").each do |key, value| %>
              <tr>
                <th><%= key %></th>
                <td>
                  <%= display_competency_property(value) %>
                </td>
              </tr>
            <% end %>
            </tbody>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
